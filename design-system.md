# Світлиця — дизайн-система

**Версія 2.0 · 14.09.2026**

**Контент:** [svitlytsia-d160da.gitlab.io](https://svitlytsia-d160da.gitlab.io/) — увесь текст узятий звідти **дослівно і не підлягає зміні**.
**Естетика:** [misto.cafe](https://misto.cafe) — колір, типографіка, форма, компоненти, ритм.
**Розкладка й фото:** [urbanspace.if.ua](https://www.urbanspace.if.ua) — повноширинні блоки, робота із зображеннями.

---

## 0. Як цим користуватися в Claude Design

Прикріпи цей файл і скажи:

> Зроби дизайн-систему та макет головної сторінки «Світлиця» за доданим файлом.
> **Увесь текст бери дослівно з §1 — не переписуй, не скорочуй, не перекладай, не додавай нових формулювань.**
> Дотримуйся палітри §2, типографічної шкали §3, ритму §4 і посекційної карти §7.

### Три правила, які не можна порушувати

1. **Текст недоторканний.** §1 — єдине джерело. Якщо в макеті потрібен напис, якого там немає, — не вигадуй його, залиш блок без напису або прибери блок.
2. **Не вигадуй цифр.** У Світлиці немає ані «80%», ані сум, ані кількості місць — і це принципово: сайт прямо каже, що напрями й суми з’являться лише після затвердження. Єдині числа, які є: **4** (напрями) і **10** (демонстраційні профілі партнерів). Великий числовий блок будується тільки на них.
3. **Дисклеймер їде разом із партнерами.** Рядок «Демонстраційні профілі. Імена й портрети вигадані.» завжди видимий поруч із портретами, а не в футері дрібним шрифтом.

---

## 1. Контент (verbatim)

### 1.0 Метадані

| Поле | Значення |
|---|---|
| `lang` | `uk` |
| `<title>` | Світлиця — кав’ярня і коворкінг у Черкасах |
| `description` | Світлиця — кав’ярня і коворкінг у Черкасах, які створює громадська організація. Твоя кава чи робочий день тут підтримують її справу. |

> **Типографічні знаки:** апостроф — `’` (U+2019), лапки — `«»`. У заголовку H1 слово **Під­трим­ка** містить м’які переноси (U+00AD) — зберігай їх, вони керують розривом рядка.

### 1.1 Навігація

| Напис | Ціль |
|---|---|
| До змісту | `#main` *(skip-link)* |
| *(логотип)* | `#top` |
| Простір | `#space` |
| Як це допомагає | `#mission` |
| Добрі справи | `#directions` |
| Прозорість | `#trust` |

### 1.2 `#hero`

- **H1:** Час для себе. Під­трим­ка для тих, хто поруч.
- **Абзац:** Світлиця — кав’ярня та коворкінг у Черкасах. Її створює громадська організація, а твоя кава чи робочий день допомагають підтримувати її справу. *(замінено замовником 17.09.2026; попередня редакція — «…і коворкінг у Черкасах, які створює громадська організація. Твоя кава чи робочий день тут підтримують її справу.»)*
- **Кнопка:** Дізнатися, як це допомагає → `#mission`
- **Примітка:** Відкриваємось у Черкасах. Слідкуй за відкриттям.
- **Фото:** `assets/images/cafe-concept-1536.webp`

### 1.3 `#model`

- **H2:** Як це працює
- **Абзац:** Ти приходиш на каву або попрацювати, заклад працює як звичайне місце для міста, а його робота підтримує справу організації «Світлиця».

### 1.4 `#space`

- **H2:** Кав’ярня і коворкінг

| H3 | Текст | Фото |
|---|---|---|
| Кав’ярня | Тепле місце для кави, розмови і паузи серед дня. | `coffee-concept-1200.webp` |
| Коворкінг | Спокійне місце для зосередженої роботи і корисних зустрічей. | `work-concept-1200.webp` |

### 1.5 `#mission`

- **H2:** Як твоя чашка допомагає
- **Абзац:** Заклад створює громадська організація «Світлиця». За проєктом статуту доходи організації не розподіляються між засновниками чи учасниками, а спрямовуються на її статутну мету.
- **Ланцюжок із трьох ланок** (порядок фіксований):
  1. Твоя чашка кави або робочий день у просторі
  2. Кошти, які отримує громадська організація
  3. Використання на статутну мету

### 1.6 `#directions`

- **H2:** Чотири напрями добрих справ

| # | H3 | Текст |
|---|---|---|
| 1 | Сильніше місто і публічні простори | Місцева інфраструктура, простори для зустрічей і спільної роботи, локальні ініціативи. |
| 2 | Підтримка людей, яких торкнулася війна | Переселенці, військові, ветерани та їхні родини. |
| 3 | Освіта, культура і спільнота | Лекції, майстерні, дискусії, зустрічі, культурні та громадські події. |
| 4 | Підтримка корисних ініціатив | Програми та адресна підтримка в межах статутної мети і затвердженого порядку. |

### 1.7 `#trust`

**Блок довіри**

- **H2:** Кому довіряти
- Громадська організація «Світлиця»
- Статут організації: проєкт.

**Питання**

- **H2:** Питання

| H3 / кнопка | Відповідь |
|---|---|
| Що таке Світлиця? | Кав’ярня і коворкінг у Черкасах, які створює громадська організація «Світлиця»: місце для кави, роботи і зустрічей. |
| Як візит допомагає? | Твоя кава чи робочий день тут підтримують справу організації, а як саме, пояснює розділ «Як твоя чашка допомагає». |
| Це бізнес чи благодійність? | Заклад створює громадська організація «Світлиця». Для відвідувача це звичайна кав’ярня і коворкінг, а сама організація залишається неприбутковою. |
| Куди йдуть гроші? | Кошти, які отримує організація, використовуються на її статутну мету. Порядок вибору конкретних справ потребує окремого затвердженого рішення, тому напрями, суми й результати з’являться тут лише після затвердження. |
| Що можна використовувати: кав’ярня і коворкінг? | Кав’ярня — місце для кави, розмови і паузи серед дня; коворкінг — спокійне робоче місце в тому самому просторі: приходиш, сідаєш за стіл і працюєш. Умови, кількість місць і ціни з’являться на цій сторінці після підтвердження. |
| Коли і де можна завітати? | Простір ще готується до відкриття в Черкасах. Дата, адреса і графік з’являться на цій сторінці після підтвердження. |

**Партнери**

- **H2:** Партнери Світлиці
- **Примітка (обов’язкова, поруч із портретами):** Демонстраційні профілі. Імена й портрети вигадані.
- **Фото:** `assets/images/partners-demo.webp` (одне зображення ×10)
- **Імена** (порядок фіксований): Олена Коваль · Андрій Левченко · Марія Бондар · Дмитро Савчук · Софія Мельник · Тарас Гончар · Анна Романюк · Олексій Ткаченко · Наталія Мороз · Максим Поліщук

### 1.8 Футер

- **H2:** Будь серед перших
- Відкриваємось у Черкасах. Слідкуй за відкриттям.
- Громадська організація «Світлиця»
- Оновлено: 11.09.2026
- **Фото:** `cafe-concept-1536.webp`

> Соцмереж, адреси, телефону, форми підписки й цін у контенті **немає**. Не додавай ці блоки.

### 1.8а Лозунги

Передані замовником 17.09.2026. Дві строфи, порядок рядків фіксований:

```
Ти прийшов за кавою.
А став частиною історії.
Оце і є «Світлиця».
```

```
Їжа для тебе.
Можливості для інших.
Світлиця — для всіх.
```

В оригіналі другу строфу завершувало ❤️. На сторінці емодзі знято: інших
емодзі на сайті немає, одне-єдине читалось би як повідомлення в чаті, а не
як голос бренду. Замість нього можна поставити фірмовий ромб — той самий,
що в `.label::before`.

**Увага:** «Їжа для тебе» — єдина згадка їжі на сайті; у решті контенту
йдеться лише про каву, розмову й роботу. Підтвердити в замовника, що заклад
справді пропонує їжу.

### 1.9 Інвентар зображень

| Файл | Зміст | Де | Пропорція |
|---|---|---|---|
| `cafe-concept-1536.webp` | Інтер’єр кав’ярні, широкий кадр | `#hero`, футер | 16:9 / 21:9 |
| `coffee-concept-1200.webp` | Кава | `#space` — Кав’ярня | 4:3 |
| `work-concept-1200.webp` | Робоче місце | `#space` — Коворкінг | 4:3 |
| `partners-demo.webp` | Демонстраційний портрет | `#trust` ×10 | 1:1 |

**Наслідок для дизайну:** фото всього **три** унікальні, а портрет один на десятьох. Тому:
- Колаж портретів (P4) працює тільки як **щільна сітка однакових плиток із вираженою маскою** — не як «різні люди». Якщо виглядає фальшиво, зроби плитки монохромними в `sand` на `ink` і залиш ім’я як основний носій.
- `cafe-concept` повторюється двічі — у футері він має бути **іншим кадром** (інший кроп, інша яскравість, інша маска), щоб не читався як дубль.
- Патерни P3 (накладена пара) і P4 будуй із того, що є; не проєктуй секцій, які вимагають нових зйомок.

---

## 2. Колір

Палітра misto.cafe. Токени названо за роллю.

### 2.1 Токени

| Токен | HEX | Роль |
|---|---|---|
| `--c-ink` | `#212121` | Текст на світлому; тло темних секцій |
| `--c-paper` | `#FFFFFF` | Основне тло |
| `--c-sand` | `#E0D8B5` | Заголовки й цифри на темному |
| `--c-signal` | `#F44E14` | Акцент: CTA, індикатори, активні стани, секції-акценти |
| `--c-forest` | `#253D13` | Другий акцент: фігури, графіка |
| `--c-surface` | `#F5F5F4` | Тло карток |
| `--c-surface-2` | `#E7E5E4` | Hover картки |
| `--c-graphite` | `#4F4F4F` | Другорядний текст |
| `--c-mute` | `#A8A29E` | Підписи, метадані |
| `--c-line` | `#D2D2D7` | Межі полів |
| `--c-rule` | `#212121` | Волосяна лінія-роздільник |
| `--c-signal-press` | `#C93D0F` | Натиснутий стан; signal-текст на світлому |
| `--c-sand-deep` | `#C9BE90` | Sand на forest |
| `--c-alert` | `#ED2024` | Помилки |

### 2.2 Теми секцій

| Тема | Тло | Заголовки | Текст | Основна кнопка |
|---|---|---|---|---|
| `light` | paper | ink | ink / graphite | surface-пігулка з signal-крапкою |
| `dark` | ink | sand | paper | signal суцільна |
| `signal` | signal | sand | ink | ink суцільна |
| `forest` | forest | sand | paper | sand суцільна, ink-текст |

**Чергування:** не більше двох `light` поспіль без темної паузи. `signal` — максимум двічі на сторінку, ніколи дві поспіль.

### 2.3 Дозволені пари (WCAG, перевірено)

✅ вільно · ⚠️ тільки ≥24px або ≥19px bold · ⛔ не текст

| Тло \ Текст | ink | paper | sand | signal | forest | graphite | mute |
|---|---|---|---|---|---|---|---|
| **paper** | 16.10 ✅ | — | 1.43 ⛔ | 3.53 ⚠️ | 11.96 ✅ | 8.19 ✅ | 2.52 ⛔ |
| **surface** | 14.76 ✅ | 1.09 ⛔ | 1.31 ⛔ | 3.24 ⚠️ | 10.97 ✅ | 7.51 ✅ | 2.31 ⛔ |
| **ink** | — | 16.10 ✅ | 11.24 ✅ | 4.56 ✅ | 1.35 ⛔ | 1.97 ⛔ | 6.38 ✅ |
| **signal** | 4.56 ✅ | 3.53 ⚠️ | 2.47 ⛔ | — | 3.39 ⚠️ | 2.32 ⛔ | 1.40 ⛔ |
| **forest** | 1.35 ⛔ | 11.96 ✅ | 8.35 ✅ | 3.39 ⚠️ | — | 1.46 ⛔ | 4.74 ✅ |
| **sand** | 11.24 ✅ | 1.43 ⛔ | — | 2.47 ⛔ | 8.35 ✅ | 5.72 ✅ | 1.76 ⛔ |

**Три пастки, у які легко впасти:**

- Signal-текст на білому дрібним кеглем — заборонено. Для тексту бери `--c-signal-press` (5.05).
- Білий на signal — тільки великим кеглем. Для дрібного — `ink`.
- Sand на signal — не читається. Sand живе на `ink` і `forest`.

**Конкретно для цього контенту:** примітка «Демонстраційні профілі. Імена й портрети вигадані.» — 14px. Якщо секція партнерів `light`, її колір — `graphite` (8.19), **не** `mute` (2.52).

---

## 3. Типографіка

### 3.1 Гарнітури

```css
--f-display: "Poppins", "Sofia Pro", ui-sans-serif, system-ui, sans-serif;
--f-text:    "Raleway", ui-sans-serif, system-ui, sans-serif;
```

| Роль | Вага | Де |
|---|---|---|
| Display | **тільки 400** | H1, H2, H3, цифри |
| Text | 400 / 600 / 700 | абзаци, кнопки, лейбли, підписи |

> Головна особливість системи: **заголовки не жирні**. Display-400 у 120px працює сильніше за Bold. Жирність живе лише в UI-тексті.
>
> Обидві гарнітури мають повну кирилицю. Перевір `’`, `«»`, `ї`, `є`, `ґ` у вибраних накресленнях.

### 3.2 Шкала

| Токен | Desktop | LH | Tracking | Гарнітура | Де в цьому контенті |
|---|---|---|---|---|---|
| `display-1` | 120px | 1.15 | −0.01em | display 400 | H1 «Час для себе…» |
| `display-2` | 78px | **0.93** | −0.02em | display 400 | «Ти приходиш на каву…» (`#model`) |
| `mega` | 180–360px | 0.8 | −0.04em | display 400 | цифра **4** у `#directions` |
| `h1` | 48px | 1.0 | −0.01em | display 400 | — |
| `h2` | 36px | 1.15 | 0 | display 400 | «Як це працює», «Кав’ярня і коворкінг», «Питання»… |
| `h3` | 28px | 1.25 | 0 | display 400 | «Кав’ярня», «Коворкінг», назви напрямів |
| `h4` | 24px | 1.3 | 0 | text 600 | питання FAQ, імена партнерів |
| `lead` | 24px | 1.5 | 0 | text 400 | абзац hero, абзац `#mission` |
| `body` | 18px | 1.55 | 0 | text 400 | описи напрямів, відповіді FAQ |
| `body-s` | 16px | 1.5 | 0 | text 400 | ланки ланцюжка `#mission` |
| `caption` | 14px | 1.45 | 0 | text 400 | «Оновлено: 11.09.2026», дисклеймер партнерів |
| `overline` | 12px | 1.2 | +0.08em UPPERCASE | text 600 | мітка над H2 |
| `button` | 16px | 1.5 | 0 | text 600 | «Дізнатися, як це допомагає» |

```css
--t-display-1: clamp(2.75rem, 8vw, 7.5rem);
--t-display-2: clamp(2.25rem, 5.4vw, 4.875rem);
--t-mega:      clamp(6rem, 25vw, 22.5rem);
--t-h1:        clamp(2rem, 3.4vw, 3rem);
--t-h2:        clamp(1.625rem, 2.5vw, 2.25rem);
--t-lead:      clamp(1.125rem, 1.7vw, 1.5rem);
--t-body:      clamp(1rem, 1.25vw, 1.125rem);
```

### 3.3 Правила набору для цього тексту

- **Довжина рядка:** `body` — 60–75 символів, `lead` — 40–55. Абзац `#mission` (223 знаки) розбивається на 4–5 рядків, не на 2.
- H1 «Час для себе. Під­трим­ка для тих, хто поруч.» — **два рядки**, розрив після «себе.». М’які переноси лишаються в розмітці як запобіжник для вузьких екранів.
- H2 «Що можна використовувати: кав’ярня і коворкінг?» — найдовше питання (44 знаки). Перевір, що воно не ламається у два рядки в акордеоні на 768px.
- Заголовок «Чотири напрями добрих справ» містить слово-число. Якщо ставиш `mega`-цифру **4**, заголовок лишається **дослівним** — цифра працює як графіка **поруч**, а не замість слова «Чотири».
- Абзаци — ліворуч. По центру — лише короткі H2 і `mega`.
- UPPERCASE — тільки `overline`. Заголовки звичайним регістром.

---

## 4. Сітка й ритм

```css
--container-max:1440px;
--container-pad:20px;    /* < 640  */
--container-pad-md:48px; /* 640–1279 */
--container-pad-lg:138px;/* ≥ 1280 — смуга 1164px */
--grid-cols:12; --grid-gap:24px;
```

Брейкпоїнти: `sm 640 · md 768 · lg 1024 · xl 1280 · 2xl 1440`.

**Шкала відступів (крок 4):** `4 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 48 · 64 · 80 · 96 · 128 · 160`

| Що | Desktop | Tablet | Mobile |
|---|---|---|---|
| Padding секції | **96px** | 64px | 48px |
| Hero (верх) | 144px | 96px | 72px |
| H2 → контент | 48px | 40px | 32px |
| Між блоками в секції | 64px | 48px | 32px |
| Заголовок → абзац | 24px | 20px | 16px |
| Gap карток | 24px | 20px | 16px |
| Шапка | 56px (+8px зазор) | 56px | 56px |

**Роздільник:** волосяна лінія `1px solid var(--c-rule)` на ширину контейнера, 96px згори й знизу. Використовується між двома `light` секціями замість зміни теми.

---

## 5. Форма

```css
--r-xs:4px;   /* інпути */
--r-sm:6px;   /* теги */
--r-md:12px;  /* фото в картці */
--r-lg:24px;  /* панелі */
--r-xl:40px;  /* картка — підпис системи */
--r-full:9999px; /* кнопки, шапка, аватари */
```

Ієрархія: **пігулка для дій, 40px для контейнерів контенту, гострий кут для фото та декору.**

Тіней майже немає — глибина будується накладанням і кольором. Виняток — плаваюча шапка:

```css
--blur-size:30px;
--glass-bg:hsla(24,6%,83%,.4);
--shadow-float:0 8px 32px rgba(0,0,0,.08);
--outline-width:2px; --outline-offset:4px;
```

---

## 6. Компоненти

### C1 · Кнопка

Пігулка, висота 48px, паддинг `0 20px`, `button`-типографіка, gap 12px.

| Варіант | light | dark | signal |
|---|---|---|---|
| Primary | surface + ink + **signal-крапка 32px** | signal + paper | ink + sand |
| Solid | signal + paper | sand + ink | ink + sand |
| Ghost | межа 1px ink | межа 1px sand | межа 1px ink |
| Text-link | signal-press + `→` | sand + `→` | ink + `→` |

```css
.btn{height:48px;padding:0 20px;border-radius:var(--r-full);
     font:600 16px/1.5 var(--f-text);display:inline-flex;
     align-items:center;gap:12px;
     transition:.3s cubic-bezier(.25,.1,.25,1)}
.btn__dot{width:32px;height:32px;border-radius:var(--r-full);
          background:var(--c-signal);flex:none;
          transition:width .3s,height .3s}
.btn:hover .btn__dot{width:40px;height:40px}
```

У контенті є **рівно одна кнопка**: «Дізнатися, як це допомагає» → `#mission`. Вона Primary. Другої CTA не вигадуй.

### C2 · Навігація

Плаваюча пігулка по центру, ширина = контейнер, `top:8px`, скло + blur. Ліворуч логотип (`#top`), праворуч чотири пункти: Простір · Як це допомагає · Добрі справи · Прозорість. Активний пункт — `signal-press`.

На мобільному — кругла signal-кнопка 48px, меню оверлеєм `dark`, пункти `h2`-кеглем по одному в рядок.

Skip-link «До змісту» → `#main`: візуально прихований, при фокусі — пігулка `signal` у лівому верхньому куті. **Не прибирай його.**

### C3 · Картка

```css
.card{background:var(--c-surface);border-radius:var(--r-xl);
      padding:32px;color:var(--c-ink)}
```

Порядок усередині: `caption` (за потреби) → `h3` → `body`. Hover — тло `surface-2`, без підйому й тіні. На `dark`: тло `rgba(255,255,255,.06)`, заголовок `sand`.

### C4 · Ланцюжок (для `#mission`)

Три ланки в ряд, між ними — `→` кольором `signal` розміром 32px. Ланка: пігулка `surface`, паддинг `20px 32px`, текст `body-s`, максимум 3 рядки.

На ≤768px — вертикально, стрілка повертається на 90°. Ланцюжок ніколи не розривається на дві лінії по 2+1.

### C5 · Акордеон (для «Питання»)

Питання — `h4` (text 600, 24px), відповідь — `body`. Роздільник між пунктами — волосяна лінія. Індикатор `+` / `−`, 24px, колір `signal`, обертання 180° за 0.3s.

Розмітка семантична: `<details>/<summary>` або кнопка з `aria-expanded`. Перший пункт може бути відкритий; решта — закриті.

### C6 · Блок-цифра

```
[ mega-цифра 4, sand на ink або signal ]
[ дослівний H2 під нею                 ]
```

Єдине застосування — `#directions`. Цифра **не замінює** слово «Чотири» в заголовку.

### C7 · Плитка партнера

Квадрат 1:1, маска P5, під нею ім’я `h4`. Ролей і описів у контенті немає — не додавай. Сітка 5×2 на десктопі, 2×5 на мобільному.

Дисклеймер «Демонстраційні профілі. Імена й портрети вигадані.» — `caption`, колір `graphite`, **над сіткою**, з відступом 24px.

### C8 · Блок довіри

`#trust`, верхня частина: `h2` «Кому довіряти», під ним два рядки — «Громадська організація «Світлиця»» (`lead`) і «Статут організації: проєкт.» (`body`, колір `graphite`). Слово **проєкт** — статус, не применшуй його візуально: це чесність, а не дрібний шрифт.

---

## 7. Посекційна карта

| # | Секція | Тема | Розкладка | Фото |
|---|---|---|---|---|
| 1 | `#hero` | `dark` | **P1** — фото на всю ширину, `display-1` поверх, кнопка й примітка внизу | `cafe-concept-1536` |
| 2 | `#model` | `light` | **L1** — одна заява `display-2`, 60% ширини, більше нічого | — |
| 3 | `#space` | `light` | **L3** — дві картки 50/50, у кожній фото `--r-md`, `h3`, `body` | `coffee-concept`, `work-concept` |
| 4 | `#mission` | `dark` | **L2** — ліворуч `h2` + `lead`, праворуч ланцюжок C4 | — |
| 5 | `#directions` | `signal` | **L6** — `mega` **4** + сітка 2×2 карток на `ink` | — |
| 6 | `#trust` а | `light` | C8 — блок довіри, під ним волосяна лінія | — |
| 7 | `#trust` б | `light` | **L2** — ліворуч sticky `h2` «Питання», праворуч акордеон C5 | — |
| 8 | `#trust` в | `dark` | **P4/C7** — дисклеймер + сітка 5×2 плиток | `partners-demo` ×10 |
| 9 | футер | `ink` | **L1** — `h2` «Будь серед перших», рядок-примітка, внизу організація + дата | `cafe-concept` (інший кроп) |

**Чому саме так:** `light → light` між 2 і 3 розділено лінією; `dark` у 4 дає паузу перед `signal` у 5; далі дві `light` і знову `dark` у 8 — ритм не провисає. `signal` вжито **один раз**.

---

## 8. Зображення

Фото ніколи не сидить рівно в прямокутнику.

### P1 · Повноширинне полотно
100vw, висота 70–100vh, без полів і радіуса. Заголовок поверх. Затемнення `linear-gradient(180deg, rgba(33,33,33,.55), rgba(33,33,33,.15))`. Текст `sand`.

### P2 · Зсунута кольорова підкладка
Під фото — суцільна фігура `signal` або `forest`, зміщена на 16–24px по діагоналі й повернута на 2–4°.

```css
.photo-offset{position:relative}
.photo-offset::before{content:"";position:absolute;inset:-16px -16px 16px 16px;
  background:var(--c-signal);transform:rotate(-2deg);z-index:0}
.photo-offset img{position:relative;z-index:1;display:block}
```

Застосування тут: дві картки `#space` — у «Кав’ярні» підкладка `signal`, у «Коворкінгу» `forest`.

### P3 · Накладена пара
Два фото, менше перекриває більше на 20–30%, повороти ±1–3°. **Не використовуй** — унікальних кадрів для цього не вистачає.

### P4 · Сітка портретів
Через один вихідний файл — це не колаж «різних людей», а **ритмічна сітка**: однакові плитки, різні маски (`--clip-quad-a` / `-b` через один), легкі повороти ±2°. Ім’я під плиткою — головний носій.

### P5 · Маска-нерівний чотирикутник
Підпис бренду. Та сама фігура працює і як помаранчева пляма під словом у заголовку.

```css
--clip-quad-a:polygon(2% 6%,97% 0%,100% 92%,5% 100%);
--clip-quad-b:polygon(0% 3%,94% 7%,100% 96%,6% 91%);
.mask-quad{clip-path:var(--clip-quad-a)}
```

### P6 · Хвиляста межа секції
Між `#mission` (`dark`) і `#directions` (`signal`) — плавна хвиля 24–60px замість прямого стику.

```html
<svg class="wave" viewBox="0 0 1440 60" preserveAspectRatio="none" aria-hidden="true">
  <path d="M0,28 C240,4 480,52 720,36 C960,20 1200,0 1440,16 L1440,60 L0,60 Z"
        fill="var(--c-signal)"/>
</svg>
```

### P7 · Поле фігур
Тло `signal`, поверх — великі плями `forest` (дуги, чверті кола), що виходять за краї. Поверх — `mega`-цифра `sand`. Фігури не перетинають текст.

**Загальні правила**

- Кольорокорекція тепла, зерно збережене, без «стокової» пластики.
- Фото в картці — радіус 12px; фото як самостійний елемент — гострий кут.
- `alt` у джерелі порожній (`alt=""`) — це декоративні зображення. Якщо фото стає змістовним (hero), напиши `alt` **описово, не копіюючи заголовок**; декоративні фігури — `aria-hidden="true"`.
- `webp` уже є; додай `loading="lazy"` всім, крім hero, і `width`/`height` для запобігання зсувам.

---

## 9. Декор

| Елемент | Опис | Де |
|---|---|---|
| Помаранчева пляма | Нерівний чотирикутник `signal` за 1–2 словами | «Під­трим­ка» в H1, «чашка» в H2 `#mission` |
| Зірка / астериск | 6–8-променева фігура `signal`, виходить за край на 30–50% | Лівий край `#directions` |
| Кружечок-булет | Коло 12px `signal` | Ланки ланцюжка C4 |
| Волосяна лінія | 1px `ink` на ширину контейнера | Між блоками `#trust` |
| Великий круг | Коло `forest`, 40–60vw, обрізане краєм | Тло `#directions` |

Максимум **два** декоративні елементи на секцію.

---

## 10. Моушн

```css
--dur:.3s; --dur-slow:.6s; --ease:cubic-bezier(.25,.1,.25,1);
```

| Подія | Анімація |
|---|---|
| Hover кнопки | крапка 32→40px, `--dur` |
| Hover картки | зміна тла, `--dur`; без `translateY`, без тіні |
| Поява секції | `opacity 0→1` + `translateY 24px→0`, `--dur-slow`, стагер 80ms |
| Поява цифри **4** | `scale .94→1` + fade, `--dur-slow` |
| Помаранчева пляма | `scale .8→1` з поворотом, `--dur-slow`, після заголовка |
| Ланцюжок `#mission` | послідовна поява ланок, стагер 120ms |
| Акордеон | висота + обертання індикатора, `--dur` |
| Стик тем секцій | не анімується — різкий, це навмисно |

`@media (prefers-reduced-motion: reduce)` — лишається тільки `opacity`.

---

## 11. Доступність

- Контраст: 4.5:1 для тексту < 24px, 3:1 для ≥24px. Матриця §2.3.
- Skip-link «До змісту» → `#main` обов’язковий і видимий при фокусі.
- Акордеон — на `<details>/<summary>` або з `aria-expanded`; питання лишається заголовком у структурі документа.
- Фокус видимий завжди: outline 2px, офсет 4px, контрастний до теми секції.
- Ціль дотику ≥ 44×44px.
- Порядок заголовків без пропусків: один H1, далі H2 на секцію, H3 усередині.
- Текст українською — рядки довші за англійські: лишай **20% запасу ширини** в кнопках і навігації.
- Скло шапки має тримати 4.5:1 для лого й пунктів над будь-яким кадром hero.

---

## 12. Do / Don't

| ✅ | ⛔ |
|---|---|
| Текст дослівно з §1 | Переписати, скоротити, «покращити» формулювання |
| Числа тільки **4** і **10** | «80%», суми, кількість місць, кількість відвідувачів |
| Дисклеймер поруч із партнерами | Дисклеймер дрібним шрифтом у футері |
| Заголовки Display **400** | Bold/Black заголовки |
| Одна CTA — «Дізнатися, як це допомагає» | Друга вигадана кнопка |
| `signal` один раз на сторінку | Помаранчеве тло в кожній другій секції |
| Чергування тем, лінія між двома `light` | Три `light` поспіль |
| Фото зі зсувом, маскою, на всю ширину | Прямокутне фото з тінню і радіусом 8px |
| Сірий текст = `graphite` (8.19) | Сірий текст = `mute` на білому (2.52) |
| Футерне фото — інший кроп | Той самий кадр двічі однаково |
| Блоки «Дата, адреса і графік з’являться після підтвердження» лишаються порожніми | Вигадана адреса, години, ціни, соцмережі |

---

## 13. tokens.css

```css
:root{
  /* колір */
  --c-ink:#212121;        --c-paper:#FFFFFF;
  --c-sand:#E0D8B5;       --c-signal:#F44E14;
  --c-forest:#253D13;
  --c-surface:#F5F5F4;    --c-surface-2:#E7E5E4;
  --c-graphite:#4F4F4F;   --c-mute:#A8A29E;
  --c-line:#D2D2D7;       --c-rule:#212121;
  --c-signal-press:#C93D0F; --c-sand-deep:#C9BE90;
  --c-alert:#ED2024;
  --c-selection:rgba(244,78,20,.2);
  --glass-bg:hsla(24,6%,83%,.4);

  /* типографіка */
  --f-display:"Poppins","Sofia Pro",ui-sans-serif,system-ui,sans-serif;
  --f-text:"Raleway",ui-sans-serif,system-ui,sans-serif;
  --t-display-1:clamp(2.75rem,8vw,7.5rem);
  --t-display-2:clamp(2.25rem,5.4vw,4.875rem);
  --t-mega:clamp(6rem,25vw,22.5rem);
  --t-h1:clamp(2rem,3.4vw,3rem);
  --t-h2:clamp(1.625rem,2.5vw,2.25rem);
  --t-h3:1.75rem; --t-h4:1.5rem;
  --t-lead:clamp(1.125rem,1.7vw,1.5rem);
  --t-body:clamp(1rem,1.25vw,1.125rem);
  --t-body-s:1rem; --t-caption:.875rem; --t-overline:.75rem;

  /* простір */
  --s-1:4px;  --s-2:8px;  --s-3:12px; --s-4:16px; --s-5:20px;
  --s-6:24px; --s-8:32px; --s-10:40px;--s-12:48px;--s-16:64px;
  --s-20:80px;--s-24:96px;--s-32:128px;--s-40:160px;
  --container-max:1440px;
  --container-pad:20px;
  --section-py:48px;   /* mobile-first */
  --header-h:56px; --header-offset:8px;

  /* форма */
  --r-xs:4px; --r-sm:6px; --r-md:12px;
  --r-lg:24px; --r-xl:40px; --r-full:9999px;
  --blur-size:30px;
  --shadow-float:0 8px 32px rgba(0,0,0,.08);
  --outline-width:2px; --outline-offset:4px;

  /* моушн */
  --dur:.3s; --dur-slow:.6s;
  --ease:cubic-bezier(.25,.1,.25,1);

  /* маски */
  --clip-quad-a:polygon(2% 6%,97% 0%,100% 92%,5% 100%);
  --clip-quad-b:polygon(0% 3%,94% 7%,100% 96%,6% 91%);
}

@media (min-width:640px){:root{--container-pad:48px;--section-py:64px}}
@media (min-width:1280px){:root{--container-pad:138px;--section-py:96px}}

.t-light {background:var(--c-paper); color:var(--c-ink)}
.t-light  h1,.t-light  h2,.t-light  h3{color:var(--c-ink)}
.t-dark  {background:var(--c-ink);   color:var(--c-paper)}
.t-dark   h1,.t-dark   h2,.t-dark   h3{color:var(--c-sand)}
.t-signal{background:var(--c-signal);color:var(--c-ink)}
.t-signal h1,.t-signal h2,.t-signal h3{color:var(--c-sand)}
.t-forest{background:var(--c-forest);color:var(--c-paper)}
.t-forest h1,.t-forest h2,.t-forest h3{color:var(--c-sand)}

::selection{background:var(--c-selection)}
.l-container{max-width:var(--container-max);margin-inline:auto;
             padding-inline:var(--container-pad)}
.c-section{padding-block:var(--section-py)}
h1,h2,h3{font-family:var(--f-display);font-weight:400}
body{font-family:var(--f-text);font-weight:400;
     font-size:var(--t-body);line-height:1.55}
```

---

## 14. Чекліст перед здачею

**Контент**

- [ ] Кожен рядок тексту звірено з §1 посимвольно
- [ ] Апострофи `’`, лапки `«»`, м’які переноси в «Під­трим­ка» на місці
- [ ] Жодного вигаданого числа, адреси, години, ціни, соцмережі
- [ ] «Демонстраційні профілі. Імена й портрети вигадані.» видно поруч із портретами
- [ ] «Статут організації: проєкт.» не применшено візуально
- [ ] Порядок 4 напрямів і 10 імен збережено
- [ ] Рівно одна кнопка на сторінці

**Дизайн**

- [ ] Жоден заголовок не жирніший за 400 у Display
- [ ] `signal`-секція одна; трьох `light` поспіль немає
- [ ] Усі пари текст/тло проходять §2.3
- [ ] Жодне фото не стоїть «просто прямокутником»
- [ ] Футерне фото — інший кроп
- [ ] Ритм 96 / 64 / 48 витриманий на трьох брейкпоїнтах
- [ ] Кнопки — пігулки 48px; картки — 40px
- [ ] Рядок ≤ 75 символів
- [ ] Skip-link працює, фокус видимий скрізь
- [ ] Перевірено на 390 / 768 / 1440px

---

*Контент: Громадська організація «Світлиця», станом на 11.09.2026 — відтворено дослівно. Естетика: misto.cafe (Луцьк) — колір, типографіка, компоненти, ритм; Urban Space 100 (Івано-Франківськ) — розкладка й робота із зображеннями. Значення токенів зняті з живих сторінок 14.09.2026.*
